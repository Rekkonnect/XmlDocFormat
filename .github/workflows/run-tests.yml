name: run-tests

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  run:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Build XmlDocFormat.InternalGenerators
      run: dotnet build ./tools/XmlDocFormat.InternalGenerators/XmlDocFormat.InternalGenerators.csproj --configuration Release

    - name: Clear NuGet Cache
      run: dotnet nuget locals all --clear

    - name: Restore Solution
      run: dotnet restore ./XmlDocFormat.slnx --verbosity detailed

    - name: Run All Tests
      run: dotnet test --solution ./
